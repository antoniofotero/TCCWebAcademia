@model IEnumerable<WebAcademia.Classes.AgendaAulas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoraInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DuracaoMinutos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoraInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DuracaoMinutos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoAgendamento }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoAgendamento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoAgendamento })
        </td>
    </tr>
}

</table>
